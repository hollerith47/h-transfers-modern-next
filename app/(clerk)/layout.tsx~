import Navbar from "@/app/(guest)/landing/GuestNavbar";

export default function Layout({children}: { children: React.ReactNode }) {
    return (
        <>
            <Navbar/>
            <div className="flex items-center justify-center pt-20 sm:pt-24">
                {children}
            </div>
        </>
    );
}